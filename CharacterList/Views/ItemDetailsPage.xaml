<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CharacterList.Views.ItemDetailsPage">
    <ContentPage.Content>
        <StackLayout>
            <Entry Text="{Binding ItemName}"
                   WidthRequest="300"
                HorizontalOptions="FillAndExpand" />
            <Entry Text="{Binding ItemType}"
                   WidthRequest="300"
                HorizontalOptions="FillAndExpand" />
            <Entry Text="{Binding SpecialTraits}"
                   WidthRequest="300"
                HorizontalOptions="FillAndExpand" />
            <Entry Text="{Binding Description}"
                   WidthRequest="300"
                   HorizontalOptions="FillAndExpand" />
            <Button Text="ADD" Command="{Binding SaveItemCommand}" CommandParameter="ADD" Margin="20" />
            <Button Text="UPDATE" Command="{Binding SaveItemCommand}" CommandParameter="UPDATE" Margin="20" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>